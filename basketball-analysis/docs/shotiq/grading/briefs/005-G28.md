# GRADE 28 — screen 005 (verify-email), iOS phone render

You are an INDEPENDENT grader. Twenty-seven have looked at this screen. The last
four returned **A−, B+, B+, B+** and between them landed **thirteen predictions
to four decimal places**, every one built rather than estimated.

**The standard, and it has three parts now (rule 105):**
1. Build an instrument and prove it reproduces the shipped raster to the byte.
2. Prove it reproduces a **recorded change** in the emitter family you intend to
   prescribe in — this file's round entries carry before-and-after numbers per
   band precisely so they can serve as regression tests.
3. Calibrate your **estimator** on that same build against a geometric
   prediction, so you know it responds to a known quantity by the right amount.

Grade 26 did (1) and (2). Grade 27 did all three and also reported a bug it found
in its own ink map. Both got predictions to four decimals.

## Inputs (all in git, all durable)

    repo root: /home/user/BasketballAnalysisAssessmentApp/basketball-analysis
    canonical: docs/shotiq/grading/canonical/005-verify-email.png   (853x1844 device px)
    render:    docs/shotiq/grading/render/005-verify-email.png      (same geometry)
    bands:     docs/shotiq/grading/render/005-report.txt
    measure:   docs/shotiq/measure/  — from docs/shotiq: `python3 -m measure.report005 <png>`
    recipe:    src/app/verify-email/phone-005.ts
    overlay:   src/app/verify-email/Marks005.tsx
    markup:    src/app/verify-email/page.tsx
    ledger:    docs/SCREEN-LEDGER.md   (METHOD RULES numbered 1-105)
    priors:    docs/shotiq/grading/briefs/005-G24.md .. 005-G27.md
    estimators: docs/shotiq/grading/briefs/r39-*.py, r40-refute.py, r41-refute.py, r42-refute.py

Run `node docs/shotiq/artefact-check.mjs` first. Current figure: **4.1236 mean|d|,
n_over8 90604**, after 42 rounds.

## The bar

A or A+ only when a thorough defect list comes back EMPTY. Say so plainly if you
find nothing that changes a pixel — that is a real result and it is what this
screen needs to hear.

## Rules 100-105. Five of the six were written by the last five rounds.

* **100** — vertical text quantised to ~1.0 device px; put your value at the
  **centre** of the plateau.
* **101** — supply a REFUTING measurement: a landmark with a tolerance, takeable
  before any band is scored.
* **102** — order matters; a self-authored instrument is not privileged; when two
  estimators disagree add a **third sharing no parameter with either**.
* **103** — **two estimators agreeing is not two independent estimators.** The
  centroid and a whole-profile SSD both mis-read a bottom-anchored height deficit
  as a translation. Paired edge crossings resolve it.
* **104** — **never sweep one half of a pair the emitter compensates.** `runCss`
  maps local y to `sy*(y+ty)`, so `sy` moves the baseline; six runs read argmin
  on a bare `sy` sweep and all six gained once `ty` moved with them.
* **105** — validate in the edit family, against a number this file recorded.

## CLOSED — grades 24-27 built ~1,450 candidates between them

Do not re-open without a mechanism and a new instrument. Each was *built*:

* **`sy` on all fifteen body runs as a 2-D (sy, ty) grid**, plus each candidate's
  cap-landing value. Eleven objective gains worth **0.0575** refused because each
  drives a cap already short — or exactly landed — further from canonical,
  including `display sy 0.99` at **−0.0129 / n8 −308**, the largest metric gain
  on the canvas.
* **Horizontal `tx` on all fifteen body runs** — thirteen at argmin.
* **`letter-spacing` on all fifteen runs**, sixty builds — argmin everywhere.
* **`ty` at ±1 device px on all fourteen body runs; `scaleX` ±0.3%/±0.6%;
  `weight` ±20/±40; `word-spacing` ±0.5/±1.0** — argmin.
* **Mark box geometry as a coupled (size, nudge) grid** on `helpIcon1`,
  `helpIcon2`, `helpIcon3`, `plateMark`, `diffMark`, `back`, the three chevrons,
  `shield` and `gear`. **`helpIcon1`, `helpIcon2`, `plateMark`, `diffMark` and
  `back` are LANDED** — every box axis inside the three estimators' own agreement
  (|stretch| ≤ 0.38, readings straddling zero). Nothing to prescribe there.
* **`shield bh`** — scores **−0.0110 / n8 −93**, the largest single gain grade 27
  found, and is a **paper-over**: the shield's interior tick is landed at +0.215
  on the shipped raster and the shrink drags it to −0.490, hiding grade 18's
  measured fat tip by pulling the whole drawing up. Refused. Do not re-find it.
* **`gear bw`** — −0.0025, refused: at its best cell the stretch it claims to fix
  still reads +0.958 of the shipped +2.354, and three estimators want 50.9 / 52.1
  / 51.5, a 1.4 px spread with no fourth to break it.
* **`diffBtn`** — Skia's supersampling blitter modelled and validated against the
  shipped raster first; joint `sw × dy × dx × dh` returns the shipped values at
  **gain 0.0000**. Residual is canonical's own resampling ringing.
* **`plate`, `BOX_Y`/`BOX_H`/box stroke, `HEADER_RULE_SPEC`, four `DIVIDER_SPEC`,
  `LINK_RULE`, `CARET`** — argmin. **Colour tokens** — graphite `#4B4D57` gains
  0.0029 with n_over8 **+17** (rule 71, refused twice).
* **Rule 50's uncovered region** — 508 rows, 570,114 px, **0 pixels over 8**.

## Closed for cause — the body face, measured on eleven runs

Grade 26 split the cap line from the x-height line, both as modal level crossings
over tens of columns (so rule 90 does not apply):

    run          cap ratio   x-height ratio        run         cap      x-height
    lede1          1.0246       1.0829             help2      0.9597     1.0226
    lede2          1.0604       1.0820             help3      0.9468     1.0327
    resendLab      1.0294       1.0995             safe1      0.9732     1.0233
    resendLink     1.0069       1.0713             safe2      0.9800     1.0505
    diffLab        0.9636       1.0216             plateLab   0.9700     1.0250
    help1          0.9434       0.9820

**For `diffLab`, `help2`, `help3`, `safe1`, `safe2` and `plateLab` the cap is
SHORT while the x-height is TALL — opposite signs, which no `scaleY` can
reconcile** because it multiplies both. Built at their cap-landing `sy`, all six
are much worse. **This is the body face and it is unreachable by any transform
this recipe emits.** Do not prescribe a vertical size on those six.

Also: **`display`** (face, mechanism measured — canonical's horizontal bars 49%
over because `scaleX` shrinks stems and leaves bars alone); **the four digits**
(canonical's Tungsten is a heavier CUT, ~478); **`shield`/`gear` curves** (asset
re-traces, grades 18/19); **`wordmark`** (canonical S/H 0.850 vs Geist 1.002);
**`didnt tx`** (five refusals, n_over8 +23, sd 1.129 > mean).

## What is open

    display    19.3019 (face, 19291 px)   diffLab    12.6948 (2801)
    digit1     10.4700 (cut)              plateLab    9.5243 (4506)
    digit0      9.4609 (cut)              safe1       8.7981 (3580)
    digit2      8.7136 (cut)              gear        8.5389 (asset)
    shield      8.5019 (asset)            diffMark    8.3575 (1179)
    digit3      8.2768 (cut)              help2       8.1496 (4503)
    didnt       7.8059 (3544)             wordmark    7.7849 (face)
    help1       6.8700 (3803)             safe2       6.6762 (3498)
    helpIcon2   6.3798 (986)              helpIcon1   6.2339 (855)
    helpIcon3   6.2137 (970)              resendLab   6.1780 (2459)
    lede2       6.1525 (3365)             help3       5.3430 (3336)
    lede1       4.9511 (3350)             plateMark   4.5255 (1468)

**No lead named.** Three briefs named leads and the grader refuted every one; the
two given none produced the best findings. Pick your own target and justify it.

Two facts, offered without recommendation:

1. **Rule 104's coupling class has now been found twice — in `runCss` and in
   `Icon`.** Both times it invalidated a whole family of earlier sweeps. The
   remaining emitters in the recipe — `markBox`'s `tx`/`ty` against `bx`/`by`,
   `hitbox`, and the `Icon` **stroke-width** compensation `sw/√(sx·sy)`, which
   couples every box edit to the drawn weight — have not been checked for the
   same shape. Read them.
2. Two graders have now found a recorded constant that was **derived from a
   measurement taken at the wrong value** and survived because everything
   downstream was self-consistent: round 33's `lede2` ascender (half the true
   figure, eight rounds) and round 20's `helpIcon3` height (three times the true
   figure, twenty-one rounds). Nobody has audited the recipe's other derived
   constants against a re-measurement of their inputs.

## What to return

Per finding: the DEFECT in one sentence; the MEASUREMENT (estimator, canonical
value, render value, why that estimator); the CAUSE; the PRESCRIPTION as literal
values **with the pivot stated**; the CONTROL — what must NOT move, plus a
rule-101 refuting measurement with a tolerance; and your CONFIDENCE.

Then ONE overall grade — A+, A, A−, B+, B, B−, or below — with the sentence that
justifies it.
