# GRADE 30 — screen 005 (verify-email), iOS phone render

You are an INDEPENDENT grader. Twenty-nine have looked at this screen. The last
six returned **A−, B+, B+, B+, B+, A−** and between them landed **eighteen
predictions to four decimal places**.

**Read this before choosing a target.** The per-round gain has collapsed:

    round   figure    gain    findings   round   figure    gain    findings
      39    4.1645   -0.0434      3        42    4.1236   -0.0069      2
      40    4.1483   -0.0162      5        43    4.1185   -0.0051      4
      41    4.1305   -0.0178      3        44    4.1176   -0.0009      1

Rounds 39–41 averaged 0.0258; rounds 42–44 average **0.0043**. Round 44 moved one
number and 43 of 44 bands were byte-identical. Meanwhile **the refusal pile has
overtaken the acceptance pile**: grade 29 refused **0.0213** and prescribed
**0.0010**, and three graders running have found their own largest-scoring
candidate to be a paper-over.

**So an honest empty list is a likely and entirely acceptable result.** If you
find nothing that changes a pixel, say so plainly and show what you checked —
that is a real finding and it is what this screen needs. Do not manufacture a
prescription to have one.

## The standard (rule 105, three parts)

1. Build an instrument and prove it reproduces the shipped raster **to the byte**
   (grade 29's control matched md5, not merely 0.0000 mean|d|).
2. Prove it reproduces a **recorded change** in the emitter family you intend to
   prescribe in. This ledger's round entries carry before-and-after numbers per
   band precisely so they can serve as regression tests.
3. Calibrate your **estimator** on that same build against a geometric
   prediction, so you know it responds to a known quantity by the right amount.

**Re-seed `variant/` from HEAD yourself and md5-verify it.** Two graders have now
inherited a stale copy carrying the previous round's edits.

## Inputs (all in git, all durable)

    repo root: /home/user/BasketballAnalysisAssessmentApp/basketball-analysis
    canonical: docs/shotiq/grading/canonical/005-verify-email.png   (853x1844 device px)
    render:    docs/shotiq/grading/render/005-verify-email.png      (same geometry)
    bands:     docs/shotiq/grading/render/005-report.txt
    measure:   docs/shotiq/measure/  — from docs/shotiq: `python3 -m measure.report005 <png>`
    recipe:    src/app/verify-email/phone-005.ts
    overlay:   src/app/verify-email/Marks005.tsx
    markup:    src/app/verify-email/page.tsx
    ledger:    docs/SCREEN-LEDGER.md   (METHOD RULES numbered 1-108)
    priors:    docs/shotiq/grading/briefs/005-G24.md .. 005-G29.md
    estimators: docs/shotiq/grading/briefs/r39-*.py, r40- .. r43-refute.py

Run `node docs/shotiq/artefact-check.mjs` first. Current figure: **4.1176 mean|d|,
n_over8 90561**, after 44 rounds.

## Rules 100-108. Seven of the nine came from the last seven rounds.

* **100** — vertical text quantised to ~1.0 device px; value at the plateau centre.
* **101** — supply a REFUTING measurement with a tolerance, takeable before any
  band is scored.
* **102** — order matters; a self-authored instrument is not privileged; when two
  estimators disagree, add a **third sharing no parameter with either**.
* **103** — **two estimators agreeing is not two independent estimators.** Paired
  edges: same direction = translation, opposite = size, one landed and one off =
  both.
* **104** — **never sweep one half of a pair the emitter compensates.** `runCss`
  maps y to `sy*(y+ty)`; `Icon` scales about the box top-left and re-weights the
  stroke. Both invalidated whole families of earlier sweeps.
* **105** — validate in the edit family, against a number this ledger recorded.
* **106** — **a fraction-of-peak crossing is not comparable across the two
  rasters.** Canonical's unsharp mask overshoots the core to coverage **1.194**.
  It reported a chevron arm 19% heavy where the truth is 2.7%, with three levels
  giving 19/13/6 — a spread wider than the quantity. Use a **fixed** level, or an
  integral with a linearity argument.
* **107 — and it is SYMMETRIC.** Re-take every landmark on the composite: a
  landmark contaminated by a sibling defect gives a real reading and a wrong
  diagnosis. It can also **license** an edit an earlier round correctly refused —
  `ry` was refused in round 43 (both edges positive: a translation) and became
  right in round 44 once `cy` removed the translation. **An edit refused earlier
  is closed only against the raster that existed then.**
* **108** — **a wrong derivation can have a right value.** `cx = 17.5 + 2.77/k`
  uses a scale this file proves wrong; correcting it consistently costs
  **+0.0051 / n8 +20**, because its input was wrong by the compensating amount.
  Audit output is a measurement per constant, never a rewrite per provenance.

## CLOSED — grades 24-29 built ~1,900 candidates between them

* **`sy` on all fifteen body runs as a 2-D (sy, ty) grid**; eleven objective gains
  worth **0.0575** refused. **`tx` on all fifteen** (thirteen argmin);
  **`letter-spacing` on all fifteen**, sixty builds; **`ty` ±1 px on fourteen**;
  **`scaleX` ±0.3/0.6%**; **`weight` ±20/40**; **`word-spacing` ±0.5/1.0**.
* **Mark box geometry as coupled (size, nudge) grids on all eleven boxes.**
  `helpIcon1`, `helpIcon2`, `plateMark`, `diffMark`, `back` LANDED.
* **All ten `Icon` stroke widths, 89 builds at a 0.12 step** — eight at argmin.
  The render's coverage takes only **0 / 63 / 127 / 190 / 254**, a **0.2480
  device-px ladder** with ~0.50-wide dead zones. A coarser sweep measures the
  ladder, not the mark.
* **The `sw`↔box coupling** — real in the emitter, **inert in this drawing set**
  (a 45° arm's width is second order in box aspect: −0.04%). Proved by geometry.
* **`shield bh`** (−0.0110, still the largest gain on the canvas) — refused three
  times, now on: fractional height error is **six different numbers by azimuth**
  (−0.19% to +2.07%, low at the shoulders and **high at the crown**), and at fixed
  rows — which `bh` cannot touch — its largest win is exactly grade 18's measured
  fat tip while the three bands above get **37% worse**.
* **`gear bh/ty`** (−0.0092) — grade 29 solved it from geometry, the grid agreed,
  and it refused its own value: the render's cog is **28.2 px tall at every side
  azimuth** where canonical runs **30.0–32.1**. Canonical draws taller lobes.
* **`helpMark1 ty`**, **`back ty`** (rule 71, n8 +5), **`ry 4.580`** (the
  objective's argmin, 74% more than the measurement asks).
* **`diffBtn`** — Skia's blitter modelled and validated; shipped values at **gain
  0.0000**. **`plate`, box rules, dividers, `LINK_RULE`, `CARET`** — argmin.
* **Colour tokens** — graphite `#4B4D57`: 0.0029 with n_over8 **+17** (rule 71).
* **Rule 50's uncovered region** — 508 rows, 570,114 px, **0 pixels over 8**.
* **Both single-derived-scale audits** — `k = 2.2257` (rule 108) and `k = 2.909`
  on `diffMark` (bottom bar row −0.116, bar end col +0.274, both landed).
* **Rule 107 survey, all eleven marks re-taken on the current composite** — every
  body edge inside 0.25 px except the two asset marks.

## Closed for cause — the body face, measured on eleven runs

    run          cap ratio   x-height ratio        run         cap      x-height
    lede1          1.0246       1.0829             help2      0.9597     1.0226
    lede2          1.0604       1.0820             help3      0.9468     1.0327
    resendLab      1.0294       1.0995             safe1      0.9732     1.0233
    resendLink     1.0069       1.0713             safe2      0.9800     1.0505
    diffLab        0.9636       1.0216             plateLab   0.9700     1.0250
    help1          0.9434       0.9820

**Six runs have a cap SHORT while the x-height is TALL — opposite signs, which no
`scaleY` can reconcile.** Built at their cap-landing `sy`, all six are much worse.
That is the body face, unreachable by any transform this recipe emits.

Also closed: **`display`** (19.3019, 21% of n_over8 — face, mechanism measured:
canonical's horizontal bars run 49% over because `scaleX` shrinks stems and
leaves bars alone); **the four digits** (canonical's Tungsten is a heavier CUT);
**`shield`/`gear` curves** (asset re-traces); **`wordmark`** (canonical S/H 0.850
vs Geist 1.002); **`didnt tx`** (five refusals).

## What is open

    display    19.3019 (face, 19291 px)   diffLab    12.6948 (2801)
    digit1     10.4700 (cut)              plateLab    9.5243 (4506)
    digit0      9.4609 (cut)              safe1       8.7981 (3580)
    digit2      8.7136 (cut)              gear        8.5389 (asset)
    shield      8.5019 (asset)            diffMark    8.3575 (1179)
    digit3      8.2768 (cut)              help2       8.1496 (4503)
    didnt       7.8059 (3544)             wordmark    7.7849 (face)
    help1       6.8700 (3803)             safe2       6.6762 (3498)
    helpIcon1   6.2339 (855)              helpIcon3   6.2137 (970)
    resendLab   6.1780 (2459)             lede2       6.1525 (3365)
    help3       5.3430 (3336)             helpIcon2   5.1229 (951)
    lede1       4.9511 (3350)             plateMark   4.5255 (1468)

**No lead named.** Five briefs have named leads and the grader refuted every one.

Two facts, offered without recommendation:

1. **Rule 107's symmetric half has been used exactly once.** Round 44 re-took one
   refused edit against the current composite and it had become right. **Every
   other refusal in this ledger was recorded against a raster that has since
   changed** — and the list of refusals is now long. Which of them were closed
   against a raster whose sibling defects have since been fixed?
2. Round 44's finding was worth 0.0010 and the round's *refusals* were worth
   0.0213. If your best candidate is in that second category, the most valuable
   thing you can return is the measurement that says so.

## What to return

Per finding: the DEFECT in one sentence; the MEASUREMENT (estimator, canonical
value, render value, why that estimator); the CAUSE; the PRESCRIPTION as literal
values **with the pivot stated**; the CONTROL — what must NOT move, plus a
rule-101 refuting measurement with a tolerance; and your CONFIDENCE.

Or return an empty list with the evidence for it.

Then ONE overall grade — A+, A, A−, B+, B, B−, or below — with the sentence that
justifies it.
