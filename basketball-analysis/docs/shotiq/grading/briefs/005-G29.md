# GRADE 29 — screen 005 (verify-email), iOS phone render

You are an INDEPENDENT grader. Twenty-eight have looked at this screen. The last
five returned **A−, B+, B+, B+, B+** and between them landed **seventeen
predictions to four decimal places**, every one built rather than estimated.

**The standard is rule 105, in three parts:**
1. Build an instrument and prove it reproduces the shipped raster to the byte.
2. Prove it reproduces a **recorded change** in the emitter family you intend to
   prescribe in — this ledger's round entries carry before-and-after numbers per
   band precisely so they can serve as regression tests.
3. Calibrate your **estimator** on that same build against a geometric
   prediction, so you know it responds to a known quantity by the right amount.

Grade 28 did all three, and when its revert disagreed with the ledger by 0.03 it
went and read the commit rather than reporting a ledger error — its own
assumption was wrong. That is the posture that gets predictions to four decimals.

## Inputs (all in git, all durable)

    repo root: /home/user/BasketballAnalysisAssessmentApp/basketball-analysis
    canonical: docs/shotiq/grading/canonical/005-verify-email.png   (853x1844 device px)
    render:    docs/shotiq/grading/render/005-verify-email.png      (same geometry)
    bands:     docs/shotiq/grading/render/005-report.txt
    measure:   docs/shotiq/measure/  — from docs/shotiq: `python3 -m measure.report005 <png>`
    recipe:    src/app/verify-email/phone-005.ts
    overlay:   src/app/verify-email/Marks005.tsx
    markup:    src/app/verify-email/page.tsx
    ledger:    docs/SCREEN-LEDGER.md   (METHOD RULES numbered 1-107)
    priors:    docs/shotiq/grading/briefs/005-G24.md .. 005-G28.md
    estimators: docs/shotiq/grading/briefs/r39-*.py, r40- .. r43-refute.py

Run `node docs/shotiq/artefact-check.mjs` first. Current figure: **4.1185 mean|d|,
n_over8 90581**, after 43 rounds.

## The bar

A or A+ only when a thorough defect list comes back EMPTY. Say so plainly if you
find nothing that changes a pixel — that is a real result and it is what this
screen needs to hear.

## Rules 100-107. Six of the eight were written by the last six rounds.

* **100** — vertical text quantised to ~1.0 device px; put your value at the
  **centre** of the plateau.
* **101** — supply a REFUTING measurement with a tolerance, takeable before any
  band is scored.
* **102** — order matters; a self-authored instrument is not privileged; when two
  estimators disagree, add a **third sharing no parameter with either**.
* **103** — **two estimators agreeing is not two independent estimators.** Paired
  edges: same direction = translation, opposite = size, one landed and one off =
  both. This is what split one `<ellipse>` into two separate findings last round.
* **104** — **never sweep one half of a pair the emitter compensates.** `runCss`
  maps y to `sy*(y+ty)`; `Icon` scales about the box top-left and re-weights the
  stroke. Both couplings invalidated a whole family of earlier sweeps.
* **105** — validate in the edit family, against a number this ledger recorded.
* **106** — **a fraction-of-peak crossing is not comparable across the two
  rasters.** Canonical's unsharp mask overshoots the core to coverage **1.194**,
  so a fraction-of-peak level sits too high on canonical and reads a thin stroke
  narrow: it reported a chevron arm **19% heavy where the truth is 2.7%**, and
  three levels of it gave 19% / 13% / 6% — a spread wider than the quantity. Use
  a **fixed** coverage level, or an integral with a linearity argument.
* **107** — **re-take every landmark on the COMPOSITE.** Measured alone, the clock
  hands' row read −0.386 and a fifth edit looked justified; measured after the
  dial's `cy` correction the same row is **+0.043, already landed**, and the "fix"
  would have driven it to +0.398.

## CLOSED — grades 24-28 built ~1,700 candidates between them

Do not re-open without a mechanism and a new instrument.

* **`sy` on all fifteen body runs as a 2-D (sy, ty) grid**; eleven objective gains
  worth **0.0575** refused, each driving a cap already short — or exactly landed —
  further from canonical. Includes `display sy 0.99` at **−0.0129 / n8 −308**.
* **`tx` on all fifteen body runs** (thirteen argmin); **`letter-spacing` on all
  fifteen**, sixty builds, argmin everywhere; **`ty` ±1 px on fourteen**;
  **`scaleX` ±0.3/0.6%**; **`weight` ±20/40**; **`word-spacing` ±0.5/1.0**.
* **Mark box geometry as a coupled (size, nudge) grid** on all eleven boxes.
  `helpIcon1`, `helpIcon2`, `plateMark`, `diffMark` and `back` are **LANDED** —
  every axis inside the estimators' own agreement.
* **`Icon` stroke widths, 89 builds at a 0.12 step** — eight of ten at argmin.
  Note the mechanism: the render's coverage takes only **0 / 63 / 127 / 190 /
  254**, a **0.2480 device-px ladder**, giving `sw` **~0.50-wide dead zones**.
  A sweep coarser than 0.12 on this axis measures the ladder, not the mark.
* **The `sw`↔box coupling is real in the emitter and inert in this drawing set** —
  a 45° arm's perpendicular width is *second order* in the box aspect (round 42's
  chevron change moved it **−0.04%**). Proved by geometry, no build needed.
* **`shield bh`** (−0.0110, the largest gain on the canvas) and **`gear bw`**
  (−0.0025) — paper-overs, refused on interior-landmark evidence.
* **`diffBtn`** — Skia's blitter modelled and validated; shipped values at
  **gain 0.0000**. **`plate`, box rules, dividers, `LINK_RULE`, `CARET`** — argmin.
* **Rule 50's uncovered region** — 508 rows, 570,114 px, **0 pixels over 8**.
* **The greedy composite from round 43** — `chev 2.40, rx 4.32, cy 18.20, hands y
  16.50` builds to **4.1167 / 90544**, better than what shipped, and was declined
  in full because each coordinate drives a landmark past canonical.

## Closed for cause — the body face, measured on eleven runs

    run          cap ratio   x-height ratio        run         cap      x-height
    lede1          1.0246       1.0829             help2      0.9597     1.0226
    lede2          1.0604       1.0820             help3      0.9468     1.0327
    resendLab      1.0294       1.0995             safe1      0.9732     1.0233
    resendLink     1.0069       1.0713             safe2      0.9800     1.0505
    diffLab        0.9636       1.0216             plateLab   0.9700     1.0250
    help1          0.9434       0.9820

**For `diffLab`, `help2`, `help3`, `safe1`, `safe2` and `plateLab` the cap is
SHORT while the x-height is TALL — opposite signs, which no `scaleY` can
reconcile.** Built at their cap-landing `sy`, all six are much worse. **This is
the body face and it is unreachable by any transform this recipe emits.**

Also: **`display`** (face, mechanism measured); **the four digits** (canonical's
Tungsten is a heavier CUT, ~478); **`shield`/`gear` curves** (asset re-traces);
**`wordmark`** (canonical S/H 0.850 vs Geist 1.002); **`didnt tx`** (five
refusals, n_over8 +23, sd 1.129 > mean).

## What is open

    display    19.3019 (face, 19291 px)   diffLab    12.6948 (2801)
    digit1     10.4700 (cut)              plateLab    9.5243 (4506)
    digit0      9.4609 (cut)              safe1       8.7981 (3580)
    digit2      8.7136 (cut)              gear        8.5389 (asset)
    shield      8.5019 (asset)            diffMark    8.3575 (1179)
    digit3      8.2768 (cut)              help2       8.1496 (4503)
    didnt       7.8059 (3544)             wordmark    7.7849 (face)
    help1       6.8700 (3803)             safe2       6.6762 (3498)
    helpIcon1   6.2339 (855)              helpIcon3   6.2137 (970)
    resendLab   6.1780 (2459)             lede2       6.1525 (3365)
    help3       5.3430 (3336)             helpIcon2   5.3367 (971)
    lede1       4.9511 (3350)             plateMark   4.5255 (1468)

**No lead named.** Four briefs have named leads and the grader refuted every one
— the last by reading the emitter rather than building, which was cheaper and
stronger. Pick your own target and justify the choice.

Three facts, offered without recommendation:

1. **`k = 2.2257` is a scale this file proves wrong, and it appears at
   `Marks005.tsx:608` and `:617` as the DERIVATION BASIS for other constants** —
   not just in the two places round 43 corrected. Grade 28's closing sentence was
   that the screen "will not be [empty] until someone re-derives every constant in
   `MailClockMark` against `sx` and `sy` rather than against `k`." That audit has
   not been done. The same question applies to any other mark whose comments cite
   a single derived scale.
2. **Three separate constants have now been found that were derived from a
   measurement taken at the wrong value and survived because everything
   downstream was self-consistent**: `lede2`'s ascender (half the true figure,
   eight rounds), `helpIcon3`'s height (three times, twenty-one rounds), the clock
   hands' offset (1.243× intended, twenty rounds). Two of the three were also
   corrections of an **extent** reading, which rule 90 refuses.
3. Rule 107 was written because a landmark was contaminated by a sibling defect.
   **Nobody has re-taken the older landmarks in this recipe on the current
   composite** — a landmark measured five rounds ago against a raster that has
   since changed four times is not evidence about today's raster.

## What to return

Per finding: the DEFECT in one sentence; the MEASUREMENT (estimator, canonical
value, render value, why that estimator); the CAUSE; the PRESCRIPTION as literal
values **with the pivot stated**; the CONTROL — what must NOT move, plus a
rule-101 refuting measurement with a tolerance; and your CONFIDENCE.

Then ONE overall grade — A+, A, A−, B+, B, B−, or below — with the sentence that
justifies it.
