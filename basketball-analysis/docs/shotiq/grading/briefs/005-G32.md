# GRADE 32 — screen 005 (verify-email), iOS phone render

You are an INDEPENDENT grader. Thirty-one have looked at this screen. The last
eight returned **A−, B+, B+, B+, B+, A−, A−, A−** and between them landed
**twenty predictions to four decimal places**.

## Your method this round is rule 110, and it is not optional

Round 46 refuted a floor claim with **one character** — `--s5-orange` was a
single unit short on green over ~80,000 px, and the correct value was already
written in the recipe's own docstring. Forty-five rounds and ~2,000 built
candidates had missed it because **every one of them swept geometry**. Rule 110:

> The next finding is usually on an axis the recipe DECLARES and no round has
> SWEPT — so enumerate what the file declares, not what previous rounds measured.
> The band table is a property of the last round's arithmetic and will not
> produce that list.

Three findings in seven rounds came from exactly this: `letter-spacing` (grade
25, declared and emitted, unswept for 40 rounds), `sy` as a 2-D grid (grade 26,
swept only as a bare 1-D sweep, which measures nothing), the colour tokens (grade
31, ~2,000 candidates of geometry and no colour audit at all).

**So begin by building the inventory, not by reading the band table.** To save
you the enumeration, here is what the source declares — I have listed it, and
deliberately measured none of it:

    Run fields (phone-005.ts):
      x  cx  top  size  weight  scale  sy  skew  ls  ws  stroke  colour
      family  dx  dy  width  bang  tx  ty  ox  oy

    swept at some point:   tx  ty  scale  sy  ls  ws  weight  size(partly)
    NEVER swept, as far as this ledger records:
      skew     — 1 use. Solved once in round 4 on `display` and never revisited,
                 and rule 104's coupling was discovered on it (CSS multiplies a
                 skewX by the scaleX that follows).
      stroke   — 0 uses. DECLARED IN THE TYPE AND USED BY NOTHING. Either it is
                 dead and should be deleted, or it is a lever nobody tried.
      family   — 9 uses.
      bang     — 1 use.
      width    — 22 uses (the cx-positioned runs' box width).
      dx / dy  — 22 uses each (the anchor offsets).
      ox / oy  — 6 uses.

    CSS custom properties (all five):
      --shotiq-color-paper  --s5-ink  --s5-graphite  --s5-orange  --s5-box-rule
      ROUND 46 audited orange (fixed) and graphite (rule-109 class, recorded not
      prescribed). Paper returns 254/254/254 at gain 0.0000 over ~1.15M px.
      --s5-ink and --s5-box-rule have NOT had a plateau audit.

    Icon: name, sw, colour, iso.  MARK_BOXES: bx by bw bh hx hy tx ty.

I am not telling you which of these to look at. Some will be dead, some closed
for cause, some genuinely unswept. **Say which you checked and what you found,
including the null results** — an unswept axis that measures clean is worth
recording so the next round does not re-derive it.

## The standard (rule 105, three parts)

1. Instrument reproduces the shipped raster **to the byte** (four graders running
   have matched md5, not merely 0.0000 mean|d|).
2. It reproduces a **recorded change** in the emitter family you prescribe in.
   Round entries carry before-and-after numbers per band for exactly this — grade
   31 recovered five bands *and two n_over8 counts* by reverting round 35.
3. Your **estimator** is calibrated on that same build against a geometric or
   arithmetic prediction.

**Re-seed `variant/` from `git show HEAD:` yourself and md5-verify against
`src/`.** Four graders in a row have found the inherited copy stale.

## Inputs

    repo root: /home/user/BasketballAnalysisAssessmentApp/basketball-analysis
    canonical: docs/shotiq/grading/canonical/005-verify-email.png   (853x1844 device px)
    render:    docs/shotiq/grading/render/005-verify-email.png
    bands:     docs/shotiq/grading/render/005-report.txt
    measure:   docs/shotiq/measure/  — from docs/shotiq: `python3 -m measure.report005 <png>`
    recipe:    src/app/verify-email/phone-005.ts
    overlay:   src/app/verify-email/Marks005.tsx
    ledger:    docs/SCREEN-LEDGER.md   (METHOD RULES numbered 1-110)
    priors:    docs/shotiq/grading/briefs/005-G24.md .. 005-G31.md
    estimators: docs/shotiq/grading/briefs/r39-*.py, r40- .. r46-refute.py

`node docs/shotiq/artefact-check.mjs` first. Current: **4.1139 / n_over8 90508**,
after 46 rounds.

## Rules 100-110 in one line each

* **100** vertical text quantised to ~1.0 device px — value at the plateau centre.
* **101** supply a refuting measurement with a tolerance, takeable before scoring.
* **102** a self-authored instrument is not privileged; two disagreeing estimators
  need a **third sharing no parameter**.
* **103** two estimators agreeing ≠ two independent estimators. Paired edges:
  same direction = translation, opposite = size.
* **104** never sweep one half of a pair the emitter compensates.
* **105** validate in the edit family, against a number this ledger recorded.
* **106** a fraction-of-peak crossing is not comparable across the two rasters.
* **107 (symmetric)** re-take every landmark on the composite — it can kill an
  edit *and* license one an earlier round correctly refused.
* **108** a wrong derivation can have a right value.
* **109** a defect can be measured and corrected exactly and still be refused by
  the objective. Mark such findings; record, do not ship.
* **110** see above.

## CLOSED — grades 24-31, ~2,000 built candidates, all geometry

`sy` (2-D grid), `tx`, `letter-spacing`, `ty`, `scaleX`, `weight`,
`word-spacing` on all fifteen body runs. All eleven mark boxes as coupled (size,
nudge) grids — `helpIcon1`, `helpIcon2`, `plateMark`, `diffMark`, `back` LANDED.
All ten `Icon` stroke widths at a 0.12 step (the raster is on a **0.2480 px
ladder** with ~0.50-wide dead zones). The stroke anisotropy (round 45's `iso`
mode — real on six marks, reachable on one; extending it without first measuring
that mark's *canonical* v/h makes `diffMark` and `helpMark2` worse). `diffBtn`
(Skia's blitter modelled; shipped values at gain 0.0000). `plate`, box rules, the
four dividers, `LINK_RULE`, `CARET`. Both single-derived-scale audits.
Rule 50's uncovered region: 508 rows, 570,114 px, **0 pixels over 8**.

Refused with measurements, do not re-find: **`shield bh`** (−0.0110, the largest
gain on the canvas — fractional height error is six different numbers by azimuth;
at fixed rows its largest win is grade 18's measured fat tip); **`gear bh/ty`**
(the render's cog is 28.2 px tall at every side azimuth where canonical runs
30.0–32.1); **`didnt tx`** (five refusals); **the chevron box aspect** (rule 109 —
measured to 0.06%, corrected exactly, refused at +0.0003); **`--s5-graphite`**
(rule 109 — three estimators give B = 77.0/77.2/78.4 against a shipped 81;
n_over8's argmin is the derived value to the unit, mean|d| declines it by
+0.0008).

## Closed for cause — the body face, measured on eleven runs

Six runs have a cap **short** while the x-height is **tall** — opposite signs no
`scaleY` reconciles, all six much worse when built at their cap-landing `sy`.
Also **`display`** (19.3019, 21% of n_over8 — face, mechanism measured), **the
four digits** (heavier Tungsten cut), **`shield`/`gear` curves** (asset),
**`wordmark`** (face).

## What is open

    display    19.3019 (face, 19291 px)   diffLab    12.6948 (2801)
    digit1     10.4700 (cut)              plateLab    9.4690 (4434)
    digit0      9.4609 (cut)              safe1       8.7981 (3580)
    digit2      8.7136 (cut)              gear        8.5389 (asset)
    shield      8.4999 (asset)            diffMark    8.3575 (1179)
    digit3      8.2768 (cut)              help2       8.1496 (4503)
    didnt       7.8059 (3544)             wordmark    7.7782 (face)
    help1       6.8700 (3803)             safe2       6.6762 (3498)
    helpIcon3   6.2137 (970)              resendLab   6.1780 (2459)
    lede2       6.1525 (3365)             helpIcon1   5.9123 (855)
    help3       5.3430 (3336)             helpIcon2   5.1229 (971)
    lede1       4.9511 (3350)             plateMark   4.4775 (1450)

## What to return

Per finding: the DEFECT in one sentence; the MEASUREMENT (estimator, canonical
value, render value, why that estimator); the CAUSE; the PRESCRIPTION as literal
values **with the pivot stated**; the CONTROL plus a rule-101 refuting
measurement with a tolerance; your CONFIDENCE. Mark rule-109-class findings.

**Plus: the axis inventory you built and what each unswept axis measured**,
including the clean ones and the dead ones.

Then ONE overall grade — A+, A, A−, B+, B, B−, or below — with the sentence that
justifies it.
